# Domain docs

This is a single-context repository.

Before relevant work, read root `CONTEXT.md` when present and applicable ADRs under `docs/adr/`. Missing files are expected; domain-modeling creates them lazily.

Use canonical terms from `CONTEXT.md`. Surface conflicts with existing ADRs rather than silently overriding them.

Expected layout:

```text
/
├── CONTEXT.md
├── docs/adr/
└── src/
```
