These are the set of changes I want to do for the next implementation. First issue abstraction. All the options. All the different options such as column size. What you demonstrate to the score, melody or lyrics, the per section versus perusus global count. The zoom. The strong and dark. I need to abstract that into a menu. So we can unbloat the parts of whenever most part of the song is only gonna show the show the menu button. The key the BPM and the temple, and the arrangement on top. So we can unbload that top section as much as possible, especially when it's on phone. But I want for everything.

Another thing is that the responsive UI whenever it's on the browser mode is fully stretched, and it's on the single column and two column mode. The items inside and the grid is resizing in a weird way. Tags disappearing. So the things inside of the band is dynamic and not the whole band itself. It's dynamic and increasing size. There should be no responsive reaction inside of the band. The whole band it's counted as one item and the whole band increases or decreases inside accordingly while keeping its proportion.

Another thing is different than the prototype. The colors on the mini section bubbles from the arrangement which is on the top of the page. They are not colored. Only when I click on them they get colored. And only when I click on them they get highlighted. Not when only when I click it similar to how our prototype is right now, reference the prototype that was created in the beginning of this project.

Also for the bar account option, I need you to add an off option. So there's off per section and global bar account option.

For the zoom, I want you to increase the zoom from fifty percent to a hundred and fifty percent. The numbers are too little right now, and you can move any step of ten each time.

The tag sections are not being sized symmetrically. So meaning that it's not the same size as the eight bar bands. While the ending tag is doing the behavior, so we have to see why is it not doing in the tag band.

So what I would like to happen is for example in the tags that only has one bar. I want the bars that are empty to be completely empty. I don't want the little dash, little white dash now. And also I don't want the bar grid. This can be misleading to think that maybe it's an empty bar. But in fact, the tag is only one bar. So I want it to represent the we'll show the first bar as it is showing right now. It closes the grid. But it's in the subsequent bar in there, which is the empty, the second, third, and the fourth grid. They should be completely empty, and no grid divider between them, but still keeping the divider for the comment on the side. So on so forth, six bars, seven bars, whatever it needs to be. It's a good indication why it's to retain this s symmetry with the other tags.

I believe from now on I want to take the melody function out of the the Chrome production the main production that I have to still prototyping to find ways to use it. You can still keep the availability on the format, but it's gonna be an in development feature that we're gonna keep on the side, and the main one is not gonna have the melody function, so we shouldn't worry about the melody function. It can be in the file, but it will not be represented at this moment in the UI. Rather than how it's showed in the format. I like the way how the dot chart is representing the melodies. It works perfectly. It's just the way that it represents it on the screen. I want to still work on it. So the main function, we're not gonna have the melody option only chords and lyrics.

So f also from now on I want to get rid of all the legacy all the legacy songs I do not need them in this iteration. Let's work only with the song that it is Mas by MiEL SaN marcos.

We can remove this the song next and previous selectors and only leave the choose song so I should so we can start the song we want. I want your brainstorm what's the possibility to us creating a future where you can pen songs and create a set list and then you can move back and forth instead of iterating to all the songs. And I want to see what's that possibility would be.

So for the lyrics view, instead of saying whenever there's no lyrics, instead of saying lyrics not available. I like us to use the information from the bar count on the chords view. And then we're gonna somehow represent how many bars the intro is that really helps singers that are looking at the lyrics. They don't need I don't want you to represent the chords there. Don't blow it with the chords. I only want you to show grid as so how many bars it's in there with the option to show the bar count, but no chord information. So we can comment what is happening and as an option, for example, guitar solo or piano instrumental, something like that, so they're aware of how long they should wait before the next section.

I want to add an option to the menu to change how in what order the tags are displayed. Because now, which is my preference. It's displayed from first column, top to bottom, second column, top to bottom. But maybe I want to give an option, which of course would only happen be useful or happen and change on the true and four column options, which is to flow from left to right always. From the beginning to the end of the song, it will flow from left to right. And we added an option to change that. So for example, if it was in the four column version, it would start intro and to the left of it verse to the left of it tag, so on and so forth.

So I believe also the directions are not showing up anymore. Even though there is a direction on the chart for mass is not showing up

